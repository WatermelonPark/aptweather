# PM → 개발 세션: '이달의 공급 통계' 화면 (2026-08-26)

`2026-08-17-타겟유저-통계루틴.md`의 B 항목을 요청서로 구체화한 것이다.
A(경계 유지 확인)는 데이터 세션이 완료했고 **게이트가 열렸다** — 지금 분리는 원천이
분리 제공 중인 것이고, 이름 충돌 감시(`4a3d6c7`)까지 들어갔다.

## 🔒 착수 표시

| 항목 | 집은 세션 | 시각(KST) | 상태 |
|---|---|---|---|
| 화면 구현 | 개발 | 09-01 23:46 | ✅ 완료 |
| 갱신 시차 확인(데이터) | | | ⬜ 미착수 |

집을 때 세션명·시각을 적고 `🔨 진행중`으로 커밋한 뒤 시작. 끝나면 `✅ 완료 <커밋>`.

## 처리 결과 (개발, 2026-09-01)

`/monthly/` 신설. 생성기 `tools/make_monthly_page.py`, 배치에 연결(TARGETS에도 추가 —
만들어도 커밋 목록에 없으면 라이브에 안 나간다).

| 요구 | 처리 |
|---|---|
| 순서 고정 | 매·전·월 → 인허가 → 입주물량 → 미분양 → 전세가율. 테스트로 잠갔다 |
| 기준월·원천 표시 | 지표마다 `<b>2026년 7월</b> 기준 · 한국부동산원 …` 한 줄 |
| 광주·전남 분리 | 20개 지역 그대로. 통합 표기가 섞이면 테스트가 실패한다 |
| 구현 형태 | 독립 경로 `/monthly/` — 블로그·카톡에서 링크 하나로 직행 |
| 측정 | `gtag('event','view',{screen_name:'monthly'})` |

지표별 기준월이 **서로 다르다**(가격 2026.07 / 인허가·미분양·전세가율 2026.06 /
입주물량 2026Q2). 하나로 뭉뚱그리지 않고 각 섹션에 그 지표의 기준월을 적었다 —
발표 주기가 원래 다르기 때문이고, 그걸 숨기면 "이번 달 발표분"이라는 확신이 거짓이 된다.

값은 원자료와 표본 대조했다: 서울 매/전/월 +1.27/+1.28/+1.12, 대구 미분양 4,383(+85),
광주 전세가율 78.4(+1.80) — 전부 data.js와 일치.

검증: 375px 가로 넘침 0, 157 passed, audit_design 위반 없음.
가드는 일부러 깨뜨려 확인했다 — 순서 뒤바꿈·기준월 제거·통합 표기·측정 제거 각각 실패.

### 데이터 세션에 남은 것
갱신 시차 확인은 그대로 열려 있다. 이 화면이 "발표 직후"를 표방하므로 그 답이
문구를 정한다 — 늦으면 "이번 달 발표분"이 아니라 "가장 최근 반영분"으로 적어야 한다.

## 무엇을 — 한 문장

매달 통계 발표 직후, **시도별 매·전·월 변동 → 인허가 → 입주물량 → 미분양(+전세가율)을
스크롤 한 번으로 훑는 화면.** 정부 사이트 4~5군데를 돌며 보는 월간 루틴을 한 화면으로
대체한다. 새 지표는 없다 — 전부 이미 우리에게 있는 데이터의 재배치다.

## 왜 — 근거는 루틴 문서에

타겟 사용자 집단이 매달 이 순서로 공개 통계를 따라 본다(실측: 전사 108편 빈도 +
최신 월간 브리핑). 불편의 핵심은 정부 **화면**이다 — 원자료에 있는 값(광주 단독 등)을
화면에서 못 찾아 손으로 합산한다. 우리는 그 값을 이미 갖고 있다.

## 요구사항

1. **순서 고정**: 시도별 매·전·월 → 인허가 → 입주물량 → 미분양 → 전세가율.
   이 순서 자체가 제품이다 — 재량으로 바꾸지 말 것.
2. **각 지표에 기준월·발표 원천 표시** — "지금 보는 게 이번 달 발표분"이라는 확신이
   이 화면의 존재 이유다.
3. **광주·전남 분리 유지 + 한 줄 주석** — 시세 계열에 "정부 화면에는 전남광주 통합으로
   표시되지만, 원자료의 시도 단위 값을 그대로 보여줍니다" 수준. 데이터 세션 회신의 표현
   ("원자료엔 있는데 화면에서 못 찾는 것을 대신 꺼내 보여준다")이 사실에 가장 가깝다.
4. **구현 형태는 개발 판단** — 독립 경로(`/monthly/` 등)든 통계 탭 안의 동선이든.
   단, 블로그·카톡에서 **링크 하나로 직행**할 수 있어야 한다(매달 발행 글의 랜딩이 된다).
5. **측정** — 기존 `view`/`to` 패턴을 따라 이 화면 진입이 GA에 잡히게.

## 금지

- 특정 인물·강의·커뮤니티 언급 일절 금지(하드 룰). "월간 루틴"류의 중립 표현만.
- 강의 일정 연동, "누가 보는 지표" 류 표현 금지 — 공개 통계의 편한 뷰까지가 우리 선이다.

## 데이터 세션 확인 1건 — 갱신 시차

미분양·인허가는 월간 발표인데 우리 배치는 주 1회다. **발표일로부터 우리 화면 갱신까지
최대 며칠 늦는지** 확인해 달라. 늦으면 "편하게"는 줘도 "발표 직후"는 못 준다 —
필요하면 발표일 즈음 배치 1회 추가를 검토(비용 대비 판단은 데이터 세션에 맡긴다).

## 마감

강제 없음. 다만 **다음 월간 발표 사이클 전에** 나오면 첫 달부터 블로그 글
("이달의 공급 통계 한 화면 정리")과 묶어 시작할 수 있다 — 그게 이상적이다.

## 검증

- 순서대로 5개 지표가 한 화면(또는 한 동선)에서 이어짐, 모바일 375px 넘침 없음
- 각 지표에 기준월 표시
- 광주·전남이 분리로 나옴
- audit_design·기존 테스트 통과, sw.js VERSION은 현재 값 읽어 +1
