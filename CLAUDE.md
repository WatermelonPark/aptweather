# 아공맵 (aptweather) 작업 지침

아파트 공급 지도. 국가 통계(KOSIS·부동산원 R-ONE·한국은행 ECOS·공공데이터포털)를 그대로 가져와
시도·시군구 단위 공급 판정과 시세를 보여주는 정적 사이트다. GitHub Pages로 배포되며 정식 도메인은
`CNAME`에 있는 것이다. **이 저장소는 공개 저장소다.** 아래 "공개 저장소 규칙"을 먼저 읽는다.

## 공개 저장소 규칙 (하드 룰)

- 개인 인명을 적지 않는다. 타겟 사용자 집단은 "부동산 스터디 커뮤니티"처럼 집단명으로만 쓴다.
- 저장소 밖 로컬 경로(원고 폴더, 강의 아카이브, 키 파일 경로 등)를 문서·코드·커밋 메시지에 적지 않는다.
- 공인 IP, API 키, 이메일을 적지 않는다. 키는 환경 변수로만 읽는다(`KOSIS_API_KEY`, `RONE_API_KEY`,
  `ECOS_API_KEY`, `DATA_GO_KR_KEY`).
- 특정 전문가·강의를 출처로 인용하지 않는다. 출처는 기관명(국토교통부, 한국부동산원, 기획재정부 등)만 쓴다.
  개념은 배우더라도 **우리 공개 데이터로 검증해 우리 근거로** 만든다.

## 구조

| 위치 | 내용 |
|---|---|
| `index.html`, `app.css`, `data.js`, `data-*.json` | 홈과 데이터 페이로드. 데이터는 `data.js`의 `ADV_DATA` 블록을 배치가 갈아끼우고 `tools/split_data.py`가 `data-*.json`으로 쪼갠다 |
| `zone/`(시도별 리포트), `weekly/`, `monthly/`, `moveins/`, `jeonse-ratio/`, `cycle/`, `share/`, 퀴즈 3종(`burini-test/`, `investor-test/`, `redev-test/`) | 생성된 정적 페이지. 손으로 고치지 말고 생성기를 고친다 |
| `about/`, `faq/`, `privacy/` | 운영 주체·설명·개인정보 페이지 |
| `tools/` | 배치·생성기·발행 도구 (아래 표) |
| `tools/tests/` | pytest. 배치가 커밋 직전에 게이트로 돌린다 |
| `tools/data/` | 시딩 데이터·캐시·스탬프. `tools/cache/`와 벌크 원본은 gitignore |
| `docs/` | 세션 간 요청서·결정 기록·백로그. 날짜 접두 파일명 |
| `.github/workflows/` | 클라우드 배치·감시·발행 알림 (아래 "자동화") |
| `drafts/`, `logs/` | **로컬 전용(gitignore)**. 블로그 초안과 배치 로그. 클라우드 세션에서는 보이지 않는다 |

### 주요 도구

| 도구 | 역할 |
|---|---|
| `update_adv_data.py --update` | 원천 API에서 데이터를 받아 `data.js`를 갱신. 키 없이 로직만 검증하려면 `--dry-run` |
| `split_data.py` | `data.js` → `data-core.js`, `data-*.json` 분리 |
| `make_sido_pages.py`, `make_indicator_pages.py`, `make_monthly_page.py` | 시도·지표·월간 페이지 생성 |
| `sido_zones.py` | 판정 단위(시도) 목록·등급·정렬의 **정본**. 순위는 반드시 `zone_order()`를 쓴다(등급군 → 절대량). 절대 세대수로 정렬하면 판정과 모순이 난다 |
| `check_freshness.py` | 데이터 신선도·정합성 검사. 감시 워크플로가 부른다 |
| `refresh_cycle_data.py` | `/cycle/` 사이클 리포트 데이터 |
| `make_zone_cards.py`, `make_og_cards.py`, `make_weekly_share.py` | 공유용 이미지·OG 카드 |
| `make_naver_post.py`, `make_theory_post.py <편번호>` | 네이버 블로그 초안 생성 → `drafts/`. 배치가 부르지 않고 사람이 발행 직전에 돌린다 |
| `naver_serp.py` | 네이버 검색 API로 색인·순위 확인(`--index`, `--track`) |
| `run_weekly_update.bat` | 로컬 배치 러너(Windows 작업 스케줄러). 실패 시 즉시 중단하고 종료 코드로 사유를 남긴다 |

### 실행

```bash
python -m pytest tools/tests -q          # 전체 테스트
python tools/update_adv_data.py --dry-run
python tools/make_sido_pages.py
```

Python 3.12. 별도 requirements 파일은 없고 표준 라이브러리 위주이며, 이미지 도구는 `pillow`를 쓴다.
Windows 콘솔에서 한글이 깨지면 `PYTHONUTF8=1`을 준다.
로컬 미리보기는 `.claude/launch.json`의 `agongmap`(정적 서버, 8321 포트)을 쓴다.

## 자동화

- **데이터 갱신**: 실운영은 로컬 러너가 맡는다(KOSIS가 해외 IP를 막아서). `update-cloud.yml`은 클라우드
  이중화로, IP 프리플라이트를 통과한 러너만 갱신을 시도하고, **커밋 직전에 pytest를 돌려 실패하면 데이터를
  커밋하지 않는다.** 테스트가 깨져 있으면 배치가 며칠째 멈춘 것으로 나타난다.
- **감시**: `watchdog.yml`이 매일 `check_freshness.py`로 라이브 데이터를 원천과 대조한다. 워크플로 자체가
  실패하면 GitHub이 실패 알림 메일을 보낸다. 배치 안의 알림 코드는 배치가 안 뜨면 실행되지 않으므로 감시자를 밖에 둔 것이다.
- **발행 알림**: `write-reminder.yml`이 화(지역·사이클 격주)·금(주간 시세) 아침에 블로그 발행 이슈를 연다.
  `publish-check.yml`이 발행을 확인하고 이슈를 닫는다.
- 클라우드 세션에는 API 키가 없다. 데이터 갱신을 실제로 돌리지 말고 `--dry-run`과 테스트로 검증한다.

## 데이터 원칙

- **숫자는 원천에서 그대로 가져온다.** 원천이 합쳐서 발표한 값을 비율로 쪼개 채우지 않는다. 검증 불가능한
  수가 되기 때문이다. 원천 분류가 바뀌면 판정 단위 자체를 바꾸거나 그 달을 보류한다.
- 사이트와 블로그가 다른 숫자·다른 산식을 말하면 안 된다. 블로그 도구는 배치가 구운 값을 읽고 재계산하지 않는다.
- **사람이 센 수와 손으로 적은 목록을 코드에 박지 않는다.** 지역 수·시군구 수·검증 대상 곳 수
  같은 값은 모델이나 데이터에서 세어 쓴다(`len(SGG_QNAME)`, `CYCLE_SYNC_N`처럼). 박아 두면 모델이
  바뀌어도 따라오지 않고 **아무것도 빨개지지 않는다.** 2026-09-10 광주·전남 통합 한 번이 아홉
  자리에서 서로 다른 모습으로 터졌다(문장 속 수, 조건의 지역 목록, 완비 기준). 정본 상수를
  만들었으면 **같은 파일 안에서도 빠짐없이 그것을 쓴다** — 제목은 상수인데 본문은 하드코딩이면
  한 글 안에서 숫자가 갈린다.
- **같은 대상을 재는 코드가 둘 이상이면 같은 상수를 쓰고 그 일치를 시험으로 고정한다.** 서로 다른
  기준으로 재면 한쪽 방어선이 다른 쪽의 고장을 덮는다(2026-09-12 분양·미분양 수집 정지).
- 계산법 공개가 핵심 가치다. 산식을 바꾸면 사이트 설명문과 블로그 초안 생성기를 같이 고친다.
- 생성 페이지의 문구는 생성기(`make_*.py`)를 고친다. 출력물을 직접 고치면 다음 배치에서 되돌아간다.

## 여러 세션이 같은 저장소를 쓴다

개발·데이터·디자인·마케팅·콘텐츠·PM·리뷰 세션이 같은 `main`에 커밋한다.

- 세션 간 연락은 `docs/` 요청서와 `docs/2026-09-09-유지보수-백로그.md`로 한다. 항목을 집을 때 세션명·시각을
  적고 `🔨 진행중` 커밋을 남기며, 끝내면 `✅ 완료`와 커밋 해시를 적는다.
- **완성한 작업은 바로 커밋한다.** 다른 세션은 내 검증 기록을 못 보므로 미커밋 상태는 "중간 상태"로 오해된다.
- 푸시 직전에 `git fetch`로 원격 상태를 확인한다. 다른 세션이 이력을 재작성하고 있을 수 있다.
- 커밋 메시지는 한국어 한 줄 요약(무엇을, 왜). 요청서 번호나 백로그 번호가 있으면 함께 적는다.

## 블로그 글 (마케팅·콘텐츠 세션)

주력 유통 채널은 네이버 블로그다. 주간 시세(금), 지역 공급(격주 화), 부동산 사이클 이론(격주 화) 세 카테고리.
운영은 3단이다: 생성기가 표·순위·링크를 채우고, Claude가 그 주 데이터의 해석 문단을 쓰고, 사용자가 검토·발행한다.

- **우리는 공급론자다.** 방어적으로 쓰지 않는다. 부정형으로 문단을 열지 않고, "다만/대신"으로 강한 사실을
  감싸지 않는다. 한계는 근거 옆에 짧게 둔다.
- **사실 나열로 끝내지 않는다.** 숫자 다음에는 "그래서 어떻게 되는가"를 쓴다. 두 갈래인데 결론이 같은 논증을
  우선 찾는다.
- **구조**: 결론 → 그 주제의 내용 → 전망·해석 → (링크) → 산식·방법론 → 면책. 방법론은 맨 뒤에 짧게.
  도구 설명서가 아니라 그 주제의 리포트로 쓴다. 소제목에 브랜드를 붙이지 않는다.
- **회차 간 반복 금지.** 매주 같은 설명(산식, 서비스 소개, 사이클 고리)을 다시 쓰지 않는다. 숫자가 지역마다
  다른 문장만 리포트 본체다.
- **지키는 선**: 투자자문 아님 면책, 시점 한계("방향은 되고 시점·폭은 안 된다"), 데이터와 견해를 가르는
  "제 판단으로는", 사이트 정본과 다른 말 금지.
- **제목**: 주간 시세는 `|` 뒤에 결론절, 지역·사이클은 쉼표. 검색결과에 30자쯤만 보이므로 앞에 검색어, 뒤에 결론.
- 사이트 문장을 그대로 옮기면 유사문서로 밀린다. 블로그는 "왜 그런가"를 사람 말로 풀고 숫자 근거는 사이트로 링크한다.
- 결론은 절대 공급 부족으로 다시 묶는다. 세제·금리 같은 외부 요인을 다룬 글일수록 그렇다.

## 응답 언어

사용자에게는 한국어로 답한다. 문장은 조사·어미를 생략하지 않은 완결된 형태로 쓰고, 명사구나 연결어미로
끝내지 않는다. 비유적 어휘 대신 일반 어휘를 쓰고, 엠대시(—)는 쓰지 않는다. 코드·주석·커밋 메시지는 프로젝트
관례를 따른다. (사용자 전역 지침 "fluent-korean"의 요약이다. 전문은 사용자 로컬 `~/.claude/CLAUDE.md`에 있다.)
